{
  "name": "ddd",
  "private": true,
  "workspaces": [
    "FRONTEND",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "start": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\" \"npx wait-on http://localhost:5173 && start http://localhost:5173\"",
    "build": "npm --workspace frontend run build",
    "preview": "npm --workspace frontend run preview",
    "lint": "npm --workspace frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "wait-on": "^9.1.0"
  }
}
